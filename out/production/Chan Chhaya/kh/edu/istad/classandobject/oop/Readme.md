**OOP**

**If we don't have constructor java-compiler will auto create default constructor when Run-time**

**Test a = new Test();**

**a** is store in stack;

**new** refer to instantiate object that reference to Test() in heap;

**Test()** is store in heap;

**instance variable = field = state = property**

**this** keyword is use for note that instance variable is for class.